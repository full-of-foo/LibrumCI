import template from './repo.html';
import controller from './repo.controller';
import './repo.styl';

let repoComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default repoComponent;

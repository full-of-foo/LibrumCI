# base from latest Jenkins-master image
FROM jenkins:1.566

# prevent requiring a Jenkins user setup
USER root
RUN apt-get update \
      && apt-get install -y sudo \
      && rm -rf /var/lib/apt/lists/*
RUN echo "jenkins ALL=NOPASSWD: ALL" >> /etc/sudoers
USER jenkins
